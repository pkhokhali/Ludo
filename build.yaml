targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          field_rename: none
      freezed:
        options:
          union_key: type
          union_value_case: snake
